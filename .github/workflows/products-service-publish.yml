name: Publish Products Service Container

on:
  push:
    branches: [ main ]
    paths:
      - 'services/products-service/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    uses: ./.github/workflows/publish-container.yml
    with:
      service_path: 'services/products-service'
      image_name: 'ghcr.io/${{ github.repository_owner }}/products-service'
      dockerfile: 'Dockerfile'
      tags: 'latest'
